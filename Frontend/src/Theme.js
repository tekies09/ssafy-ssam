// 테마를 여기서 정의하시면 됩니다.

import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#37467B',
    },
    secondary: {
      main: '#638493'
    },
  }
})

export default theme